/* 
 * This file has been automatically generated by the jrl-cmakemodules.
 * Please see https://github.com/jrl-umi3218/jrl-cmakemodules/blob/master/config.hh.cmake for details.
*/

#ifndef MASTER_BOARD_SDK_CONFIG_HH
# define MASTER_BOARD_SDK_CONFIG_HH

// Package version (header).
# define MASTER_BOARD_SDK_VERSION_UNKNOWN_TAG 0 // Used to mention that the current version is unknown.
# define MASTER_BOARD_SDK_VERSION "UNKNOWN-dirty"
# define MASTER_BOARD_SDK_MAJOR_VERSION MASTER_BOARD_SDK_VERSION_UNKNOWN_TAG
# define MASTER_BOARD_SDK_MINOR_VERSION MASTER_BOARD_SDK_VERSION_UNKNOWN_TAG
# define MASTER_BOARD_SDK_PATCH_VERSION MASTER_BOARD_SDK_VERSION_UNKNOWN_TAG

#define MASTER_BOARD_SDK_VERSION_AT_LEAST(major, minor, patch) (MASTER_BOARD_SDK_MAJOR_VERSION>major || (MASTER_BOARD_SDK_MAJOR_VERSION>=major && \
                                                             (MASTER_BOARD_SDK_MINOR_VERSION>minor || (MASTER_BOARD_SDK_MINOR_VERSION>=minor && \
                                                                                                     MASTER_BOARD_SDK_PATCH_VERSION>=patch))))

#define MASTER_BOARD_SDK_VERSION_AT_MOST(major, minor, patch) (MASTER_BOARD_SDK_MAJOR_VERSION<major || (MASTER_BOARD_SDK_MAJOR_VERSION<=major && \
                                                            (MASTER_BOARD_SDK_MINOR_VERSION<minor || (MASTER_BOARD_SDK_MINOR_VERSION<=minor && \
                                                                                                     MASTER_BOARD_SDK_PATCH_VERSION<=patch))))

// Handle portable symbol export.
// Defining manually which symbol should be exported is required
// under Windows whether MinGW or MSVC is used.
//
// The headers then have to be able to work in two different modes:
// - dllexport when one is building the library,
// - dllimport for clients using the library.
//
// On Linux, set the visibility accordingly. If C++ symbol visibility
// is handled by the compiler, see: http://gcc.gnu.org/wiki/Visibility
# if defined _WIN32 || defined __CYGWIN__
// On Microsoft Windows, use dllimport and dllexport to tag symbols.
#  define MASTER_BOARD_SDK_DLLIMPORT __declspec(dllimport)
#  define MASTER_BOARD_SDK_DLLEXPORT __declspec(dllexport)
#  define MASTER_BOARD_SDK_DLLLOCAL
# else
// On Linux, for GCC >= 4, tag symbols using GCC extension.
#  if __GNUC__ >= 4
#   define MASTER_BOARD_SDK_DLLIMPORT __attribute__ ((visibility("default")))
#   define MASTER_BOARD_SDK_DLLEXPORT __attribute__ ((visibility("default")))
#   define MASTER_BOARD_SDK_DLLLOCAL  __attribute__ ((visibility("hidden")))
#  else
// Otherwise (GCC < 4 or another compiler is used), export everything.
#   define MASTER_BOARD_SDK_DLLIMPORT
#   define MASTER_BOARD_SDK_DLLEXPORT
#   define MASTER_BOARD_SDK_DLLLOCAL
#  endif // __GNUC__ >= 4
# endif // defined _WIN32 || defined __CYGWIN__

# ifdef MASTER_BOARD_SDK_STATIC
// If one is using the library statically, get rid of
// extra information.
#  define MASTER_BOARD_SDK_DLLAPI
#  define MASTER_BOARD_SDK_LOCAL
# else
// Depending on whether one is building or using the
// library define DLLAPI to import or export.
#  ifdef master_board_sdk_EXPORTS
#   define MASTER_BOARD_SDK_DLLAPI MASTER_BOARD_SDK_DLLEXPORT
#  else
#   define MASTER_BOARD_SDK_DLLAPI MASTER_BOARD_SDK_DLLIMPORT
#  endif // MASTER_BOARD_SDK_EXPORTS
#  define MASTER_BOARD_SDK_LOCAL MASTER_BOARD_SDK_DLLLOCAL
# endif // MASTER_BOARD_SDK_STATIC
#endif //! MASTER_BOARD_SDK_CONFIG_HH
