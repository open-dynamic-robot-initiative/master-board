name: CI - Nix Master board SDK

on: [push, pull_request]

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { submodules: recursive }
      - uses: cachix/install-nix-action@v30
      - run: nix build .?submodules=1
